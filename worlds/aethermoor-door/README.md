The playable Aethermoor Door pack lives in the door repo, not in this tree:

`/home/clawd/dev/talesmud-door/worlds/aethermoor-door`

`config/door.yaml` sets `world_pack` to that directory (screens, ANSI, monsters, weapons, armor). This folder is only a pointer so the engine repo does not keep a second copy of the prose.
